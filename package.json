{
  "name": "xela-account-manager",
  "version": "3.0.0",
  "description": "Offline, encrypted local vault for accounts, notes, cards, keys and more",
  "homepage": "./",
  "private": true,
  "author": "fnetflix464-dot",
  "license": "MIT",
  "dependencies": {
    "@tauri-apps/api": "^2.9.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "webdriverio": "^9.30.0"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "scripts": {
    "react-start": "react-scripts start",
    "react-build": "react-scripts build",
    "start": "tauri dev",
    "test": "react-scripts test --watchAll=false --passWithNoTests",
    "test:watch": "react-scripts test --passWithNoTests",
    "test:e2e": "tauri build --no-bundle && node e2e-tauri/run.mjs",
    "build": "tauri build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
